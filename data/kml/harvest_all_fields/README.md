# Harvest KML files

Place **253** field KML files here, renamed to internal ID format:

```text
IND-KA-600044.kml
IND-KA-600048.kml
...
```

Geometry is read from these paths. Database `file_name` uses the canonical name from `data/mapping/_harvest_all_manifest.csv` (not the on-disk rename).
